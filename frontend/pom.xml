<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <artifactId>misc-page-frontend</artifactId>
    <name>Miscellaneous Page Frontend</name>

    <packaging>pom</packaging>

    <parent>
        <artifactId>misc-page-root</artifactId>
        <groupId>cdv.mp</groupId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <properties>
        <npm.executable>npm</npm.executable>
        <npm.arguments>install --loglevel=error</npm.arguments>
        <gulp.executable>gulp</gulp.executable>
        <gulp.arguments/>
        <build.directory>${project.basedir}/build</build.directory>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-resources</id>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <outputDirectory>${build.directory}</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.basedir}/src/main/resources</directory>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <configuration>
                    <workingDirectory>${build.directory}</workingDirectory>
                </configuration>
                <executions>
                    <execution>
                        <id>run-npm</id>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <executable>${npm.executable}</executable>
                            <commandlineArgs>${npm.arguments}</commandlineArgs>
                        </configuration>
                    </execution>
                    <execution>
                        <id>run-gulp</id>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <executable>${gulp.executable}</executable>
                            <commandlineArgs>${gulp.arguments}</commandlineArgs>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>run-on-windows</id>
            <activation>
                <os>
                    <family>windows</family>
                </os>
            </activation>
            <properties>
                <shell.script>${project.basedir}/shell.bat</shell.script>
                <npm.executable>${shell.script}</npm.executable>
                <npm.arguments>npm install --loglevel=error</npm.arguments>
                <gulp.executable>${shell.script}</gulp.executable>
                <gulp.arguments>gulp</gulp.arguments>
            </properties>
        </profile>
    </profiles>

</project>